import Foundation
import SpriteKit
import GameplayKit

var score: Int = 0
var lives: Int = 5


//                snLabel.text = "\(round(spriteNode.physicsBody?.velocity.dx ?? CGFloat(0) * 10) / 10.0), \(round(spriteNode.physicsBody?.velocity.dy ?? CGFloat(0) * 10) / 10.0)"

//                snLabel.text = "\(round(spriteNode.physicsBody?.velocity.dx ?? CGFloat(0) * 10) / 10.0), \(spriteNode.physicsBody?.velocity.dy ?? CGFloat(0))"
//                snLabel.position = CGPoint(x: 0, y: 0)
//                addChild(snLabel)

//        var snLabel: SKLabelNode = SKLabelNode()
